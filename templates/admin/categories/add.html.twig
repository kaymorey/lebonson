{% extends "layout/admin.html.twig" %}
{% block title %}Ajouter - Catégories{% endblock %}
{% block content %}
<div class="row">
	<div class="span12">
		<h3>Catégories <small>AJOUTER UNE CATÉGORIE</small></h3>
		<a class="btn btn-success" href="index.php"><i class="icon-circle-arrow-left icon-white"></i> Retour aux catégories</a>
		<hr />
		{% if errors %}
		<div class="alert alert-error">
			{% for error in errors %}
				{{ error }}
				{% if loop.last == false %}
				<br />
				{% endif %}
			{% endfor %}
		</div>
		{% endif %}
		<form action="add.php" method="post">
			<label for="title">Nom *</label>
			<input type="text" name="title" id="title" value="{{ title_post }}" />
			<br />

			<input type="submit" class="btn btn-primary" value="Valider" />
			<br />
			<small>Les champs suivis d'un astérisque sont obligatoires.</small>
		</form>
	</div>
</div>
{% endblock %}